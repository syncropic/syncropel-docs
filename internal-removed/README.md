# internal-removed

Pages in this folder were removed from the public documentation site. They
describe internal workflow, infrastructure, or implementation detail and must
not be served. They are kept here only so nothing is lost; they are outside
`content/docs`, so the site build does not pick them up, and they are excluded
from the vocabulary lint.
