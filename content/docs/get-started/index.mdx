---
title: Get Started
description: The installation-to-live-instance journey for Syncropel. Install the CLI, initialize your identity, start your instance, and pair devices.
---

This section takes you from "I just heard about Syncropel" to "I have a secure instance running with a browser paired to it." It is intentionally more detailed than the [Quickstart](/docs/start) — read Quickstart if you want the 10-minute happy path; read this section if something went wrong on your first try, or if you're setting up a machine that will do real work.

## Map

| # | Page | What it covers |
|---|---|---|
| 0 | [Pricing](/docs/get-started/pricing) | Local is free; hosted is $25 per month per instance with a 30-day trial. |
| 1 | [Hosted instance signup](/docs/get-started/hosted) | Skip install — sign up at syncropel.com and get a fully-portable instance running in under a minute. |
| 2 | [Install](/docs/get-started/install) | Download and install `spl` on Linux, Windows, or macOS. Pin versions. Verify. |
| 3 | [First run](/docs/get-started/first-run) | `spl init`, your identity, the `~/.syncro/` tree, and `config.toml`. |
| 4 | [Pairing a browser or phone](/docs/get-started/pairing) | QR pairing, manual pairing, revocation. |
| 5 | [Troubleshooting](/docs/get-started/troubleshooting) | Diagnostic tree for the failure modes real users have hit. |
| 6 | [Reset and uninstall](/docs/get-started/reset-uninstall) | Clean reset, full uninstall per platform, what survives. |

## Suggested reading order

**First time, want a hosted instance.** Read [Hosted instance signup](/docs/get-started/hosted) — the syncropel.com flow gets you to a working instance without installing anything. Then `spl token save <bearer>` to get the CLI talking to your hosted instance, then jump to [Pairing](/docs/get-started/pairing) for additional devices.

**First time installing locally.** Start at [Install](/docs/get-started/install), proceed through [First run](/docs/get-started/first-run), then [Pairing](/docs/get-started/pairing) once you want a browser or second device on the same instance.

**Pairing a second device.** Jump to [Pairing](/docs/get-started/pairing).

**Something is broken right now.** [Troubleshooting](/docs/get-started/troubleshooting) first, then [Reset and uninstall](/docs/get-started/reset-uninstall) if you need to start over.

## What "Syncropel" means on your machine

`spl` is a single binary. Once installed, it is both the CLI (`spl task list`, `spl intend …`) and the server (`spl serve`). They communicate over a local Unix socket on Linux and macOS, and over TCP on Windows. The server persists everything it knows to `~/.syncro/hub.db`, a SQLite file.

That's the whole system. No Docker required, no separate language runtime, no external database. The pages in this section are about getting that one binary installed correctly and pointing it at a well-formed `~/.syncro/` directory.

## See also

- [Quickstart](/docs/start) — the 10-minute walkthrough
- [Operator Runbook](/docs/operate/runbook) — day-2 operations once your instance is doing real work
- [`spl doctor`](/docs/operate/doctor) — one-command diagnostic when an instance is misbehaving
