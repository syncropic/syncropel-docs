---
title: API Proxy
description: Observe every LLM call — model, tokens, cost, latency
---

## Overview

`spl serve` includes a transparent API proxy that records every LLM call. Point any SDK at `localhost:9100` and get full observability.

### Supported APIs

| API | Endpoint | Status |
|-----|----------|--------|
| Anthropic Messages | `POST /v1/messages` | Production |
| Anthropic Token Count | `POST /v1/messages/count_tokens` | Production |
| OpenAI Chat Completions | `POST /v1/chat/completions` | Production |

## What gets recorded

Each API call produces two records. The proxy stores metadata in each record's `proxy` field:

**Request record** (when you send a prompt):
- Your message (first 2000 chars)
- Model name
- Message count in context

**Response record** (when the LLM responds):
- The response (first 2000 chars)
- Model used
- Input and output token counts
- Estimated cost in USD
- Round-trip latency in milliseconds

## Try it

```bash
# With curl (Anthropic format)
curl http://localhost:9100/v1/messages \
  -H "x-api-key: $ANTHROPIC_API_KEY" \
  -H "content-type: application/json" \
  -H "anthropic-version: 2023-06-01" \
  -d '{"model":"claude-sonnet-4-6","max_tokens":100,"messages":[{"role":"user","content":"Hello"}]}'

# See the dashboard
open https://syncropel.com/local
```

## Cost estimation

Costs are estimated per-model:

| Model | Input ($/1M tok) | Output ($/1M tok) |
|-------|-------------------|---------------------|
| claude-opus-4-6 | $15.00 | $75.00 |
| claude-sonnet-4-6 | $3.00 | $15.00 |
| claude-haiku-4-5 | $0.80 | $4.00 |
| gpt-4o | $2.50 | $10.00 |
| gpt-4o-mini | $0.15 | $0.60 |

## Health endpoint

`GET /health` includes proxy statistics:

```json
{
  "proxy": {
    "total_calls": 142,
    "total_cost_usd": 4.20,
    "total_input_tokens": 45230,
    "total_output_tokens": 12840,
    "models_used": ["claude-sonnet-4-6"],
    "last_call_at": "2026-03-29T00:01:23Z"
  }
}
```

## Streaming

Both streaming and non-streaming calls are supported. Streaming responses are forwarded immediately — the proxy never buffers the full response.

## Security

- The proxy runs on localhost only (not exposed to the network)
- Your API key is forwarded via `x-api-key` header (never stored in records)
- Records truncate prompt/response to 2000 chars
- Daemon requires Bearer token for mutating endpoints
- Rate limited: 120 requests/minute per client IP

## What's Next

- [Console Dashboard](/docs/guides/console) — See your calls in real-time
- [Local Registry](/docs/guides/local-registry) — Storage, API, configuration
- [Troubleshooting](/docs/reference/troubleshooting) — Common proxy issues
